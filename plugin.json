{
  "uuid": "b7f3a1d2-5c48-4e19-9a6b-0d3e8f2c5a71",
  "id": "publish_to_paprika",
  "version": 1,
  "githubURL": "https://github.com/JuergenB/typingmind-publish-to-paprika",
  "emoji": "\ud83c\udf36",
  "title": "Paprika Recipes",
  "iconURL": "https://raw.githubusercontent.com/JuergenB/typingmind-publish-to-paprika/main/icon.png",
  "authenticationType": "AUTH_TYPE_NONE",
  "overviewMarkdown": "Gives the chef agent hands in [Paprika Recipe Manager 3](https://www.paprikaapp.com/): it can look through the recipes you already have, read one, revise it after you have cooked it, and publish a new one with a photo you choose.\n\n| Function | What it does |\n|---|---|\n| `find_recipes` | Searches the recipes already in your Paprika by ingredient |\n| `get_recipe` | Reads one recipe in full, by uid |\n| `update_recipe` | Revises a recipe you already have. **Overwrites - there is no undo** |\n| `preview_recipe_images` | Draws several food-photography options and returns their URLs, so you see them and pick one *before* the recipe is published |\n| `publish_recipe` | Writes a new recipe into Paprika, with the photo you chose, and tells your devices to sync immediately |\n\nIt reads, adds and revises recipes. It does not delete anything, and it does not touch meal plans, the grocery list or the pantry.\n\n## Settings\n\n**Service URL** arrives filled in. **Service Token** is the `SERVICE_TOKEN` from the deployment's environment, and is the only thing you have to type - re-importing the plugin creates a fresh copy with it empty, so it needs pasting again each time.\n\nLeave it running **server-side**, which is the default. That is what makes it work from a phone with nothing installed, and what keeps the token off your devices.",
  "userSettings": [
    {
      "name": "serviceUrl",
      "label": "Service URL",
      "description": "Base URL of the paprika-aware-chef deployment, with no trailing slash. For example https://paprika-aware-chef.vercel.app",
      "type": "text",
      "required": true,
      "defaultValue": "https://paprika-aware-chef.vercel.app"
    },
    {
      "name": "serviceToken",
      "label": "Service Token",
      "description": "The SERVICE_TOKEN from the deployment's environment. This is the only setting you have to type. Re-importing the plugin creates a fresh copy with this empty, so it has to be pasted again after every import.",
      "type": "password",
      "required": true
    }
  ],
  "pluginFunctions": [
    {
      "id": "get_recipe",
      "name": "get_recipe",
      "implementationType": "http",
      "outputType": "respond_to_ai",
      "httpAction": {
        "id": "36cfac50-65e0-4556-b697-9c1429a512b1",
        "url": "{serviceUrl}/v1/recipes/get",
        "name": "",
        "method": "POST",
        "hasBody": true,
        "hasHeaders": true,
        "requestBody": "{\n  \"uid\": \"{uid}\"\n}",
        "requestHeaders": "{\n  \"Authorization\": \"Bearer {serviceToken}\",\n  \"Content-Type\": \"application/json\"\n}",
        "requestBodyFormat": "json"
      },
      "openaiSpec": {
        "name": "get_recipe",
        "description": "Read one recipe from Paprika in full, by uid. find_recipes returns only names and the lines that matched; this returns the whole thing.\n\nUse it when the user wants to revise something they have cooked, or to look at a recipe the search turned up. Keep the hash it returns and pass it back to update_recipe.",
        "parameters": {
          "type": "object",
          "required": [
            "uid"
          ],
          "properties": {
            "uid": {
              "type": "string",
              "description": "The recipe's uid, exactly as find_recipes returned it. Never a name - names are not unique and this is how you address the right recipe."
            }
          }
        }
      }
    },
    {
      "id": "update_recipe",
      "name": "update_recipe",
      "implementationType": "http",
      "outputType": "respond_to_ai",
      "httpAction": {
        "id": "125903fd-5a23-4c14-aa1b-7dcbd5684904",
        "url": "{serviceUrl}/v1/recipes/update",
        "name": "",
        "method": "POST",
        "hasBody": true,
        "hasHeaders": true,
        "requestBody": "{\n  \"uid\": \"{uid}\",\n  \"expected_hash\": \"{expected_hash}\",\n  \"name\": \"{name}\",\n  \"description\": \"{description}\",\n  \"servings\": \"{servings}\",\n  \"prep_time\": \"{prep_time}\",\n  \"cook_time\": \"{cook_time}\",\n  \"difficulty\": \"{difficulty}\",\n  \"ingredients\": \"{ingredients}\",\n  \"directions\": \"{directions}\",\n  \"notes\": \"{notes}\",\n  \"nutritional_info\": \"{nutritional_info}\",\n  \"source\": \"{source}\",\n  \"source_url\": \"{source_url}\",\n  \"categories\": \"{categories}\",\n  \"rating\": \"{rating}\"\n}",
        "requestHeaders": "{\n  \"Authorization\": \"Bearer {serviceToken}\",\n  \"Content-Type\": \"application/json\"\n}",
        "requestBodyFormat": "json"
      },
      "openaiSpec": {
        "name": "update_recipe",
        "description": "Revise a recipe ALREADY in Paprika. This overwrites it and there is no undo - Paprika keeps no version history.\n\nONLY call this once the user has explicitly agreed to the change. Read the recipe with get_recipe first, talk the revision through, and confirm the recipe's title back to them before writing. A wrong uid overwrites the wrong recipe with no way back.\n\nOnly the fields you send are changed; anything you leave out keeps its current value, which is what protects the photo and the categories. Send the COMPLETE new text for any field you change - it replaces rather than merges.\n\nSay in notes what changed and why, so the recipe does not silently differ from the one they cooked.",
        "parameters": {
          "type": "object",
          "required": [
            "uid"
          ],
          "properties": {
            "uid": {
              "type": "string",
              "description": "The recipe's uid, from find_recipes or get_recipe."
            },
            "expected_hash": {
              "type": "string",
              "description": "The hash get_recipe returned. Send it back. If the recipe has changed elsewhere since you read it, the update is refused instead of quietly overwriting the other change."
            },
            "name": {
              "type": "string",
              "description": "A new title, if it should change."
            },
            "description": {
              "type": "string",
              "description": "\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable."
            },
            "servings": {
              "type": "string",
              "description": ""
            },
            "prep_time": {
              "type": "string",
              "description": ""
            },
            "cook_time": {
              "type": "string",
              "description": ""
            },
            "difficulty": {
              "type": "string",
              "description": ""
            },
            "ingredients": {
              "type": "string",
              "description": "The COMPLETE revised ingredients, not just the changed lines - this replaces the field. Same rules as publishing: grouped by cooking stage under headings ending in a colon, one ingredient per line, quantity first, no bullet characters.\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable."
            },
            "directions": {
              "type": "string",
              "description": "The COMPLETE revised directions, not just the changed part.\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable."
            },
            "notes": {
              "type": "string",
              "description": "The COMPLETE revised notes. When you revise a recipe after the user has cooked it, say here what changed and why - 'halved the sugar, it was too sweet at the original quantity'. A recipe that silently differs from the one they cooked is worse than no revision.\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable."
            },
            "nutritional_info": {
              "type": "string",
              "description": "\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable."
            },
            "source": {
              "type": "string",
              "description": ""
            },
            "source_url": {
              "type": "string",
              "description": ""
            },
            "categories": {
              "type": "array",
              "description": "Replacement category NAMES. Left out, the existing ones are kept.",
              "items": {
                "type": "string"
              }
            },
            "rating": {
              "type": "integer",
              "description": "0 to 5. Left out, the existing rating is kept."
            }
          }
        }
      }
    },
    {
      "id": "find_recipes",
      "name": "find_recipes",
      "implementationType": "http",
      "outputType": "respond_to_ai",
      "httpAction": {
        "id": "910bc9db-cbad-43ad-a494-8d8130ab7ed0",
        "url": "{serviceUrl}/v1/recipes/search",
        "name": "",
        "method": "POST",
        "hasBody": true,
        "hasHeaders": true,
        "requestBody": "{\n  \"ingredient\": \"{ingredient}\",\n  \"limit\": \"{limit}\"\n}",
        "requestHeaders": "{\n  \"Authorization\": \"Bearer {serviceToken}\",\n  \"Content-Type\": \"application/json\"\n}",
        "requestBodyFormat": "json"
      },
      "openaiSpec": {
        "name": "find_recipes",
        "description": "Search the recipes ALREADY in the user's Paprika for ones that use a given ingredient. Read-only: it cannot change or delete anything.\n\nCall this whenever the user asks what they already have - 'do I have anything with cabbage', 'have I made this before', 'what can I do with the yuzu' - and before developing a new recipe when knowing their collection would make your answer better, for instance to avoid proposing a near duplicate of something they already cook.\n\nReturns the recipe names and the ingredient lines that matched. Tell the user the names; quote the matching lines only where they help. You cannot open a recipe's full text - if they want that, they open it in Paprika.\n\nFast - well under a second - because the collection is indexed. Very occasionally the index has to be rebuilt and the call takes about a minute; if that happens, say the search is running rather than assuming it failed.",
        "parameters": {
          "type": "object",
          "required": [
            "ingredient"
          ],
          "properties": {
            "ingredient": {
              "type": "string",
              "description": "The ingredient to look for, e.g. 'cabbage', 'chicken thighs', 'yuzu'. One ingredient at a time. Matching is on whole words and is not case-sensitive, and it searches the ingredient lines of every recipe in the account as well as the titles."
            },
            "limit": {
              "type": "integer",
              "description": "Most recipes to return. 20 is usually plenty."
            }
          }
        }
      }
    },
    {
      "id": "publish_recipe",
      "name": "publish_recipe",
      "implementationType": "http",
      "outputType": "respond_to_ai",
      "httpAction": {
        "id": "b58be3de-d53c-4b53-9a0d-8b180de000ff",
        "url": "{serviceUrl}/v1/recipes",
        "name": "",
        "method": "POST",
        "hasBody": true,
        "hasHeaders": true,
        "requestBody": "{\n  \"name\": \"{name}\",\n  \"description\": \"{description}\",\n  \"servings\": \"{servings}\",\n  \"prep_time\": \"{prep_time}\",\n  \"cook_time\": \"{cook_time}\",\n  \"difficulty\": \"{difficulty}\",\n  \"ingredients\": \"{ingredients}\",\n  \"directions\": \"{directions}\",\n  \"notes\": \"{notes}\",\n  \"nutritional_info\": \"{nutritional_info}\",\n  \"source\": \"{source}\",\n  \"source_url\": \"{source_url}\",\n  \"categories\": \"{categories}\",\n  \"rating\": \"{rating}\",\n  \"generate_image\": \"{generate_image}\",\n  \"image_prompt\": \"{image_prompt}\",\n  \"image_url\": \"{image_url}\"\n}",
        "requestHeaders": "{\n  \"Authorization\": \"Bearer {serviceToken}\",\n  \"Content-Type\": \"application/json\"\n}",
        "requestBodyFormat": "json"
      },
      "openaiSpec": {
        "name": "publish_recipe",
        "description": "NEWLINES: in every text field below, write line breaks as the two characters backslash-n, never by pressing Enter. A real line break makes the request body invalid JSON and the call fails inside the plugin before it is sent.\n\nPublish a completed recipe to the user's Paprika Recipe Manager, optionally with a generated food-photography image. ONLY call this once the user has explicitly asked for the recipe to be published - 'publish it', 'send it to Paprika', or an equally plain yes. Finishing the recipe is NOT permission to publish it; the user develops recipes in conversation and decides when one is ready. Send the recipe as it stands after every revision agreed in the conversation, not the first draft. Do not print the recipe as markdown in the chat as well - publishing is the delivery.",
        "parameters": {
          "type": "object",
          "required": [
            "name",
            "ingredients",
            "directions"
          ],
          "properties": {
            "name": {
              "type": "string",
              "description": "The recipe title."
            },
            "description": {
              "type": "string",
              "description": "One or two factual sentences saying what the dish is and what is in it. No flourish.\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable and the recipe never leaves the chat. Example, exactly as it should be sent: 'Sear the chicken:\\\\n1 tablespoon olive oil\\\\n\\\\nBuild the sauce:\\\\n2 cloves garlic'. The service turns those back into real line breaks."
            },
            "servings": {
              "type": "string",
              "description": "Free text, e.g. '4' or '6 to 8'. Use the number the user asked for."
            },
            "prep_time": {
              "type": "string",
              "description": "Free text, e.g. '15 minutes'."
            },
            "cook_time": {
              "type": "string",
              "description": "Free text, e.g. '40 minutes'. Paprika computes the total itself, so do not send one."
            },
            "difficulty": {
              "type": "string",
              "enum": [
                "Easy",
                "Medium",
                "Hard",
                ""
              ],
              "description": "Leave empty if unsure."
            },
            "ingredients": {
              "type": "string",
              "description": "One ingredient per line, newline separated.\n\nGROUP BY COOKING STAGE, in the order the stages happen. Start each group with a heading line ending in a colon ('Sear the chicken:'), then a blank line before the next group. Paprika renders a line ending in a colon as a section heading, so the list reads in cooking order.\n\nWhen one ingredient is split across stages, list it in each stage at the amount that stage needs, and note the total on its first appearance: '1 tablespoon olive oil, of 2 tablespoons total'. Do NOT also list it as a separate total line - Paprika's grocery list adds the lines up, and a total line plus the per-stage lines would buy twice the oil.\n\nThe quantity MUST come first on every ingredient line ('400 grams tonnarelli', never 'tonnarelli, 400 grams') because that is what Paprika's portion scaling reads. NEVER begin a line with a bullet, dash, asterisk or number - it breaks Paprika's parser. Headings are the one exception to quantity-first, and they must end with a colon.\n\nFor metric alternatives write the imperial line, then a line containing only the word 'or', then the metric line.\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable and the recipe never leaves the chat. Example, exactly as it should be sent: 'Sear the chicken:\\\\n1 tablespoon olive oil\\\\n\\\\nBuild the sauce:\\\\n2 cloves garlic'. The service turns those back into real line breaks."
            },
            "directions": {
              "type": "string",
              "description": "Grouped by phase. Put the phase name alone on a line in bold (**Sear the meat**), then the steps as prose paragraphs beneath it. Blank line between paragraphs and before each phase heading. No numbered steps, no bullets. Include exact temperatures, vessel sizes and sensory cues.\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable and the recipe never leaves the chat. Example, exactly as it should be sent: 'Sear the chicken:\\\\n1 tablespoon olive oil\\\\n\\\\nBuild the sauce:\\\\n2 cloves garlic'. The service turns those back into real line breaks."
            },
            "notes": {
              "type": "string",
              "description": "Chef's tips, substitutions, make-ahead and storage instructions.\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable and the recipe never leaves the chat. Example, exactly as it should be sent: 'Sear the chicken:\\\\n1 tablespoon olive oil\\\\n\\\\nBuild the sauce:\\\\n2 cloves garlic'. The service turns those back into real line breaks."
            },
            "nutritional_info": {
              "type": "string",
              "description": "Free text. Leave empty unless the user asked for it.\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable and the recipe never leaves the chat. Example, exactly as it should be sent: 'Sear the chicken:\\\\n1 tablespoon olive oil\\\\n\\\\nBuild the sauce:\\\\n2 cloves garlic'. The service turns those back into real line breaks."
            },
            "source": {
              "type": "string",
              "description": "Where the recipe came from. Use 'Paprika Aware Chef' for recipes you developed."
            },
            "source_url": {
              "type": "string",
              "description": "URL of the original, when adapting a published recipe."
            },
            "categories": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Category names, not IDs, e.g. ['Pasta', 'Italian']. The service creates any that do not exist yet."
            },
            "rating": {
              "type": "integer",
              "description": "0 to 5. Leave at 0 for a new recipe the user has not cooked yet."
            },
            "generate_image": {
              "type": "boolean",
              "description": "Whether to generate a food-photography image. Set this from the user's answer, never assume."
            },
            "image_prompt": {
              "type": "string",
              "description": "How the finished plated dish should look: ingredients visible, plating, natural window light, shallow depth of field, restaurant presentation. Photographic only - never illustrated or stylized."
            },
            "image_url": {
              "type": "string",
              "description": "The URL of the image the user chose from preview_recipe_images. Pass it back exactly as given. When this is set the chosen image is used as-is and generate_image is ignored, so the picture published is the one they actually picked."
            }
          }
        }
      }
    },
    {
      "id": "preview_recipe_images",
      "name": "preview_recipe_images",
      "implementationType": "http",
      "outputType": "respond_to_ai",
      "httpAction": {
        "id": "e5841840-0ed3-48f0-9d48-c0ef226e3073",
        "url": "{serviceUrl}/v1/recipe-images",
        "name": "",
        "method": "POST",
        "hasBody": true,
        "hasHeaders": true,
        "requestBody": "{\n  \"name\": \"{name}\",\n  \"subject\": \"{subject}\",\n  \"surface\": \"{surface}\",\n  \"serving\": \"{serving}\",\n  \"count\": \"{count}\"\n}",
        "requestHeaders": "{\n  \"Authorization\": \"Bearer {serviceToken}\",\n  \"Content-Type\": \"application/json\"\n}",
        "requestBodyFormat": "json"
      },
      "openaiSpec": {
        "name": "preview_recipe_images",
        "description": "NEWLINES: in every text field below, write line breaks as the two characters backslash-n, never by pressing Enter. A real line break makes the request body invalid JSON and the call fails inside the plugin before it is sent.\n\nGenerate several food-photography options for a recipe and return their URLs, so the user can see them and choose one BEFORE the recipe is published. Call this when the user has asked to publish and said they want a photo.\n\nDescribe the FOOD only, in the three fields subject, surface and serving. Every word about photography - camera, lens, light, plate, grain - is supplied by the service and is the same on every recipe, so do not write any of it here.\n\nWrite those fields from the recipe you just developed, NOT from the dish's name. A title says 'charred cabbage'; the camera needs to know it is a two-inch wedge rather than half a head. Give real dimensions, how many pieces, how each was cut, and what the cut surface reveals.\n\nState what things ARE. Never write an exclusion such as 'not bok choy' or 'no sauce' - image models have no negation and weight up whatever you name, so an exclusion summons the thing you meant to avoid. Exclusions are stripped before the prompt is sent.\n\nTakes roughly 20 to 30 seconds, so tell the user the photos are being drawn. Show every returned URL as a numbered markdown image, ask which they want, then call publish_recipe passing their choice as image_url. The links expire after about an hour.",
        "parameters": {
          "type": "object",
          "required": [
            "name",
            "subject"
          ],
          "properties": {
            "name": {
              "type": "string",
              "description": "The recipe title."
            },
            "subject": {
              "type": "string",
              "description": "THE OBJECT ON THE PLATE, physically. The single most important field. Name the principal component, how many pieces, how it was cut or formed, and its actual size with a real measurement. Then say what the cut or broken surface reveals - layers, grain, crumb, filling. Example: 'One wedge of green cabbage, a triangular slab two inches thick at its wide leafy edge tapering to a point, about five inches long, one of four cut through the core from half a head. The flat cut face shows long pale layers fanning from a firm creamy core strip along the bottom edge.' A reader who has never seen the dish should be able to draw it from this.\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable and the recipe never leaves the chat. Example, exactly as it should be sent: 'Sear the chicken:\\\\n1 tablespoon olive oil\\\\n\\\\nBuild the sauce:\\\\n2 cloves garlic'. The service turns those back into real line breaks."
            },
            "surface": {
              "type": "string",
              "description": "WHAT THE COOKING DID TO THE SURFACE. Be specific about the evidence the method leaves, because that is what makes a picture read as food rather than as a render. Char is uneven blistering and near-black patches with pale untouched food between, not an even brown coat. A sear is a dry crust. Steaming leaves things moist and slumped. A reduced glaze is glossy and clings; a thin sauce pools flat. Example: 'The cut face is blistered and blackened in uneven patches from a dry cast-iron pan, pale green and untouched between the scorch marks, edges dried and papery.'\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable and the recipe never leaves the chat. Example, exactly as it should be sent: 'Sear the chicken:\\\\n1 tablespoon olive oil\\\\n\\\\nBuild the sauce:\\\\n2 cloves garlic'. The service turns those back into real line breaks."
            },
            "serving": {
              "type": "string",
              "description": "HOW IT IS SERVED: the vessel, the sauce and where it sits, the garnish and how much. Use the quantities the recipe actually calls for - a teaspoon of sesame seeds is a scatter, not a blanket. Example: 'On a shallow stoneware plate, a thin dark soy-yuzu glaze pooled around the base, a scatter of thinly sliced scallion and toasted sesame.'\n\nCRITICAL, or the call fails before it is sent: write every line break as the two characters backslash and n. Do NOT press Enter inside this field. The plugin host pastes your text straight into a JSON body without escaping it, so one real line break makes the whole request unparseable and the recipe never leaves the chat. Example, exactly as it should be sent: 'Sear the chicken:\\\\n1 tablespoon olive oil\\\\n\\\\nBuild the sauce:\\\\n2 cloves garlic'. The service turns those back into real line breaks."
            },
            "count": {
              "type": "integer",
              "description": "How many options to generate, 1 to 4. Use 4 unless the user asks otherwise: roughly one frame in three still gets the cut or the thickness wrong, so a fourth is what makes it near-certain one of them is right."
            }
          }
        }
      }
    }
  ],
  "disabled": false
}
